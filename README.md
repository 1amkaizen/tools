# tools automation ffuf,wfuzz

clone repository
```bash
git clone https://github.com/1amkaizen/tools.git
```
```bash
cd tools
```

only file
```bash
sudo chmod +x *
```
copy all file,folder command,and payload dont
```bash
sudo cp * /usr/local/bin
```

Running

```bash
$ automations
```
